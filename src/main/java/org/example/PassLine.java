package org.example;

public class PassLine extends Bet{
    public PassLine(int decisionValue, int betAmount) {
        super(decisionValue, betAmount);
    }

    @Override
    public int resolve(int diceRoll, int point) {
        if (this.decisionValue )
        return 0;
    }
}
